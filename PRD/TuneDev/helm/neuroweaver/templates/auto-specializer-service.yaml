apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-auto-specializer
  labels:
    app: {{ .Release.Name }}-auto-specializer
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    environment: {{ .Values.environment }}
spec:
  type: {{ .Values.service.autoSpecializer.type }}
  ports:
    - port: {{ .Values.service.autoSpecializer.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-auto-specializer
    release: {{ .Release.Name }}