apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-costguard-dashboard
  labels:
    app: {{ .Release.Name }}-costguard-dashboard
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    environment: {{ .Values.environment }}
spec:
  type: {{ .Values.service.costguardDashboard.type }}
  ports:
    - port: {{ .Values.service.costguardDashboard.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-costguard-dashboard
    release: {{ .Release.Name }}