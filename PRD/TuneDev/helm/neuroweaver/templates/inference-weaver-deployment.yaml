apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-inference-weaver
  labels:
    app: {{ .Release.Name }}-inference-weaver
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    environment: {{ .Values.environment }}
spec:
  replicas: {{ .Values.replicaCount.inferenceWeaver }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-inference-weaver
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-inference-weaver
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: inference-weaver
          image: {{ .Values.imageRegistry }}/neuroweaver/inference-weaver:{{ .Values.imageTag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8003
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: {{ .Values.environment }}
            - name: S3_BUCKET
              value: {{ .Values.s3.bucket }}
            - name: AWS_REGION
              value: {{ .Values.s3.region }}
            - name: REDIS_HOST
              value: {{ .Values.redis.host }}
            - name: REDIS_PORT
              value: "{{ .Values.redis.port }}"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-redis-credentials
                  key: password
          resources:
            limits:
              cpu: {{ .Values.resources.inferenceWeaver.limits.cpu }}
              memory: {{ .Values.resources.inferenceWeaver.limits.memory }}
            requests:
              cpu: {{ .Values.resources.inferenceWeaver.requests.cpu }}
              memory: {{ .Values.resources.inferenceWeaver.requests.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          volumeMounts:
            - name: models-volume
              mountPath: /app/models
            - name: checkpoints-volume
              mountPath: /app/checkpoints
      volumes:
        - name: models-volume
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-models-pvc
        - name: checkpoints-volume
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-checkpoints-pvc
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
      {{- end }}